import Head from 'next/head';

const AppHead = () => {
	return (
		<Head>
			<meta name="msapplication-TileColor" content="#000" />
			<meta name="theme-color" content="#000" />
			<title>Mightsea</title>
		</Head>
	);
};

export default AppHead;
